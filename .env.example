# Clerk — authentication.
# Values come from https://dashboard.clerk.com (app "Vertex"), or `clerk env pull`.
# Only the publishable key may reach the browser. The secret key is server only.
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=
CLERK_SECRET_KEY=
